---
permalink: /404.html
---
> **Note:** You accessed a link that returned a 404, probably by clicking one of the plugin links. You're supposed to copy the link address and add it into Revenge.

# Avatar Override (Revenge plugin)

指定したユーザーやサーバーの **アバター・表示名・サーバーアイコン・サーバー名** を、**自分の端末上の表示だけ**好きな内容に差し替えるRevengeプラグインです。
差し替えはローカル表示のみで、相手や他の閲覧者・サーバーには一切送信・共有されません。Discord本来のデータは変わりません。

## インストール方法

1. Revengeの `設定 > プラグイン` を開きます。
2. 右上の「+」からリポジトリURLとして次を追加します。
   ```
   https://hihei56.github.io/revenge-avatar-override/avatar-override/
   ```
3. 一覧に出てくる `Avatar Override` をインストールして有効化します。
4. プラグインの設定画面には4つのセクションがあります。
   - **アバターを追加**: ユーザーID + 画像URL (URLを空欄にすると、ランダムなうんこ画像が自動で割り当てられます)
   - **ユーザー名を追加**: ユーザーID + 表示させたい名前
   - **サーバーアイコンを追加**: サーバーID + 画像URL
   - **サーバーバナー画像を追加**: サーバーID + 画像URL (チャンネル一覧の一番上、サーバー名の上に表示される横長のバナー画像)
   - **サーバーホームヘッダー画像を追加**: サーバーID + 画像URL (ホーム/ガイドタブ上部のヘッダー画像)。サーバーIDの代わりに `default` と入力すると、個別設定のない全サーバー共通のヘッダー画像になります
   - **サーバー名を追加**: サーバーID + 表示させたいサーバー名
   - **チャンネル名を追加**: チャンネルID + 表示させたいチャンネル名 (個別指定、下の一括変更より優先されます)
   - **時計チャンネルを追加**: チャンネルIDを登録すると、そのチャンネルの名前が常に現在時刻 (例: 🕐 14:32) に置き換わります。チャンネル名指定より優先されます。約30秒ごとに更新を試みますが、Discord側の再描画タイミング次第でずれることがあります
   - **予定までのカウントダウン**: チャンネルID + 予定日時 (`YYYY-MM-DD HH:mm` 形式) + 任意のラベルを設定すると、そのチャンネルの名前が「⏳ 残り2時間15分」のように予定までの残り時間表示に変わります。時計チャンネルと同じチャンネルには両方は設定できません (後から設定した方が優先されます)
   - **ロール表示**: 「プロフィールのロールを非表示にする」「ロールアイコンを非表示にする」の2つのスイッチ。サーバー単位ではなく全サーバー共通のON/OFFで、プロフィール画面などのロール一覧・ロールアイコンをまとめて非表示にできます (自分自身のロール一覧は、権限判定に使われるデータのため常に対象外です。ロールアイコンのみ自分にも適用されます)
   - **ロール非表示から除外するユーザーを追加**: ここに登録したユーザーIDだけは、上の2つのスイッチがONでもロール・ロールアイコンが通常通り表示されます (自分自身や特定の人だけ除外したい場合に使います)
   - **サーバー内の全チャンネル名を一括変更**: サーバーID + 共通の名前。そのサーバー内の全チャンネルが同じ名前で表示されます
   - **ロールカラー無効化サーバーを追加**: サーバーIDを登録すると、そのサーバー内のメンバー名の色が全員デフォルト色になります。スイッチでON/OFF切り替え可能 (長押しでリストから削除)
   - **オンラインステータスを隠すユーザーを追加**: ユーザーIDを登録すると、そのユーザーは常にオフライン扱いで表示されます (本人には気づかれません)。スイッチでON/OFF切り替え可能 (長押しでリストから削除)
   - **サーバー内全員をオフライン扱いにするサーバーを追加**: サーバーIDを登録すると、そのサーバーのメンバー全員が常にオフライン扱いになります。オンラインステータスはユーザー単位の共通データのため、対象者はそのサーバーに限らずDMや他のサーバーでも常にオフライン扱いになる点に注意してください
   - **一括変更から除外するユーザーを追加**: ここに登録したユーザーIDは、下の「サーバー内の全ユーザー/Bot一括変更」系の設定すべてから常に除外されます (自分自身を除外したい場合などに使います)
   - **サーバー内の全ユーザーのアイコンを一括変更**: サーバーID + 画像URL (空欄でランダムなうんこ画像)。除外ユーザーと個別のアバター上書きがあるユーザーを除き、そのサーバー内の全員のアバターが一律で変わります
   - **サーバー内の全ユーザーの表示名を一括変更**: サーバーID + 表示名 (全員共通)。除外ユーザーを除き、そのサーバー内の全員の表示名 (ニックネーム) が一律で変わります
   - **サーバー内のBot/webhookアイコンを一括変更**: サーバーID + 画像URL (空欄でランダムなうんこ画像)。そのサーバー内のwebhookメッセージのアバターに一括適用されます。実在のBotアカウント (Carl-botなど、サーバーの実メンバーであるbot) は自動的に対象外になります
   - **表示を許可するサーバータグを追加**: サーバーID (タグの元サーバー) を1つ以上登録すると、それ以外のサーバー由来のサーバータグ (メッセージ・プロフィールでユーザー名の横に出る小さなタグ) が全て非表示になります。何も登録しなければ通常通りです
5. 一覧の項目をタップすると編集・削除ができます。
6. 「設定のバックアップ」セクションから、現在の全設定をJSONとしてエクスポート (クリップボードにコピー + OSの共有シートでファイル保存やメモアプリへの転送も可能) したり、以前エクスポートしたJSONを貼り付け/クリップボードから読み込んで復元したりできます。機種変更時のバックアップや、設定の使い回しに使えます。

画像URLが必要な項目には「端末から画像を選択」ボタンがあり、URLを自分で用意しなくても端末内の画像を直接選べます。ただしこれは端末内のファイルパス (`file://...`) を使うため、**その端末でのみ**有効です。他の端末やエクスポートした設定を別端末にインポートしても、選んだ画像は反映されません (再度その端末で選び直してください)。他端末でも使い回したい場合は、これまで通り画像URLを直接入力してください。

ユーザーID/サーバーIDは、対象のプロフィールやサーバー設定から「IDをコピー」(開発者モードが必要) で取得してください。

> GitHub Pagesの反映には初回のみ数分かかることがあります。上記URLが404になる場合は少し待ってから再度お試しください。

## 仕組み

以下の内部モジュールにパッチを当てて、登録済みのID (ユーザーID / サーバーID) に一致した場合だけローカルに保存した内容を返すようにしています。

- `getUserAvatarURL` / `getUserAvatarSource` / `UserStore.getUser` → アバター・表示名・サーバータグ (`user.primaryGuild.identityGuildId`が許可リストに無ければ`primaryGuild`を消去)
- `getGuildIconURL` / `getGuildIconSource` / `getGuildBannerURL` / `getGuildBannerSource` / `getGuildHomeHeaderURL` / `getGuildHomeHeaderSource` → サーバーアイコン・サーバーバナー画像・サーバーホームヘッダー画像
- React Native の`Image`コンポーネント自体 (`cdn.discordapp.com` / `media.discordapp.net` のURLパターンを直接検知) → 上記の関数パッチが効かない画面向けの保険。サーバーアイコン・ホームヘッダーの実際のCDN URLパターンに一致した場合、表示直前でURLを差し替えます
- `GuildStore.getGuild` → サーバー名
- `ChannelStore.getChannel` → チャンネル名 (個別指定 / サーバー単位の一括指定 / 時計・カウントダウン)。時計・カウントダウンが設定されている間は、約30秒ごとに`ChannelStore.emitChange()` (Discordの内部データ変更通知の仕組み) を呼び出して表示の更新を促しています
- `GuildMemberStore.getMember` の `colorString` / `colorRoleId` / `colorStrings` (グラデーションロール用) を消去、`nick` を上書き → ロールカラー無効化 / サーバー内全ユーザーの表示名一括変更。同じ箇所で `roles` を空配列に、`iconRoleId` を消去 → ロール表示・ロールアイコン非表示
- `PresenceStore.getStatus` → オンラインステータス非表示 (登録ユーザー、または登録サーバーの実メンバーは常に`offline`を返す)
- `User`インスタンスの`getAvatarURL(guildId, ...)` / `getAvatarSource(guildId)` → サーバー内の全ユーザー/webhookアイコン一括変更。`bot`フラグに加えて、そのサーバーの`GuildMemberStore.getMember`に実在するか (webhookは実際のメンバーではないため存在しない) で実在Botとwebhookを区別しています (個別のアバター上書きと除外ユーザーは常に対象外)
- チャット画面のアバターは上記のインスタンスメソッドを経由せず、サーバー情報を受け取らない`getUserAvatarURL`/`getUserAvatarSource`だけを呼ぶため、そちらにも`SelectedGuildStore.getGuildId()`(今開いているサーバー)を使って同じ一括変更ロジックを適用しています
- `getGuildMemberAvatarURL` / `getGuildMemberAvatarSource` → 「サーバーごとのアバター (サーバープロフィール画像)」を設定しているユーザー向け。これは通常の`getUserAvatarURL`系とは別経路で解決されるため、そのユーザーがサーバー内で個別アバター上書き/一括変更の対象になっていても、これを別途パッチしないと反映されませんでした

アバターにGIFのURLを登録すると、アニメーション表示に対応した箇所ではアニメーションGIFとして、それ以外では静止画として表示されます。
アバターURLを空欄のまま追加すると、Twemoji/Noto/OpenMoji/Fluent Emoji/Appleなど複数種類の💩絵文字画像からランダムに1つが割り当てられます。

保存データはプラグインのストレージ (`@vendetta/plugin` の `storage`) に保持されます。

## ソースからビルドする場合

```sh
npm install
npm run build
```

`dist/avatar-override/` にビルド済みの `manifest.json` と `index.js` が出力されます。
`main` ブランチへのpushで `.github/workflows/deploy.yml` が自動的にビルドし、GitHub Pagesへデプロイします
(pushイベントで自動起動しない場合は、GitHubのActionsタブから手動で "Run workflow" してください)。

## 注意事項

- Discordのアプリ内部実装の変更で、パッチが効かなくなる可能性があります。
- モバイル版Discord (Revenge) 専用です。デスクトップ版Discordのmod (Vencordなど) では動作しません。
- 画像URLはリンク先のホストが生きている必要があります。
- サーバー名/サーバーアイコンの変更は、そのサーバーの画面を開いたときに読み込まれるデータに対するローカルなパッチです。実際のサーバー設定は変わりません。
